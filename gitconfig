[user]
	name = Hei
	email = lamyuenhei@gmail.com
[core]
	editor = vim
[color]
	diff = auto
	ui = true
[alias]
	co = checkout
	cm = commit
	st = status -sb
	br = branch
	graph = log --graph --all --format=format:'%C(bold blue)%h%C(reset) - %C(white)%s%C(reset) %C(bold white)— %C(reset)%C(bold cyan)%an%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)' --abbrev-commit --date=relative
	gr = graph
	rb = rebase
	ri = rebase -i
	ss = show --stat --summary
	sw = switch
	rs = restore
[merge]
	tool = vimdiff
	conflictstyle = diff3
[rebase]
	autosquash = true
[pull]
	ff = only
[diff]
    tool = difftastic
    external = difft
[difftool]
    prompt = false
[difftool "difftastic"]
    cmd = difft "$LOCAL" "$REMOTE"
[init]
	defaultBranch = main
